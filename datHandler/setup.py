from setuptools import setup, find_packages

setup(
    name='datHandler',
    version='0.0.1',
    packages=find_packages(),
    author='Gruppo-1',
    description='Data handling package',
    install_requires=[
        'pandas',
    ]
)